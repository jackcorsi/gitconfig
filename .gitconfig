[user]
	email = jackcorsiwarren@gmail.com
	name = Jack
	signingkey = 7325FFDEAA304292
[core]
	editor = subl -w
[alias]
	c = checkout
	s = status
	b = branch
	cb = checkout -b
	amend = commit --amend
	prb = pull --rebase
	hrst = reset --hard
	irb = rebase --interactive
	pret = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)'
